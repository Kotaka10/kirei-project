export type FileInfoTypes = {
    id: number;
    fileName: string;
    storedFileName: string;
    storedPath: string;
    mimeType: string; // ファイルタイプ（image/pngなど）
    size: number;
    url: string;
};
